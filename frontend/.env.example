# Copie este arquivo para ".env" e ajuste se necessário.
# Este valor é público por natureza (vai para o bundle do navegador) —
# é só a URL onde o backend está rodando, nunca coloque segredos aqui.

NEXT_PUBLIC_API_URL="http://localhost:3333"
